[Unit]
Description=Salt Security Agent - Celery Beat scheduler
Documentation=https://github.com/
After=network.target redis.service salt-agent-worker.service
Wants=redis.service

[Service]
Type=simple
User=salt-agent
Group=salt-agent
WorkingDirectory=/etc/salt-agent
ExecStart=/usr/local/bin/salt-agent beat --config /etc/salt-agent/config.toml --loglevel INFO
Restart=on-failure
RestartSec=5

KillMode=mixed
TimeoutStopSec=30

# Hardening. Beat writes its schedule database; by default Celery places it
# in the working directory (celerybeat-schedule). Adjust ReadWritePaths if
# you point Beat at a different location.
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/etc/salt-agent

[Install]
WantedBy=multi-user.target
